import 'package:flutter/material.dart';

class UpdatePage2 extends StatelessWidget {
  const UpdatePage2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("글수정"),
      ),
    );
  }
}
